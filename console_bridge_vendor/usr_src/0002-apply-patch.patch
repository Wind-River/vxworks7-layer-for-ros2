From 3e3aa1d9a1cd57c6ff92ae906b7cd2004e94be03 Mon Sep 17 00:00:00 2001
From: Andrei Kholodnyi <andrei.kholodnyi@gmail.com>
Date: Wed, 24 Feb 2021 11:22:15 +0100
Subject: [PATCH] apply patch

---
 CMakeLists.txt | 7 +++++++
 1 file changed, 7 insertions(+)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index 7a9e709..b86ce6d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -31,6 +31,12 @@ macro(build_console_bridge)
   if(DEFINED CMAKE_BUILD_TYPE)
     list(APPEND extra_cmake_args -DCMAKE_BUILD_TYPE=${CMAKE_BUILD_TYPE})
   endif()
+  if(DEFINED CMAKE_MODULE_PATH)
+    list(APPEND extra_cmake_args -DCMAKE_MODULE_PATH=${CMAKE_MODULE_PATH})
+  endif()
+  if(DEFINED BUILD_TESTING)
+    list(APPEND extra_cmake_args -DBUILD_TESTING=${BUILD_TESTING})
+  endif()
   if(DEFINED CMAKE_TOOLCHAIN_FILE)
     list(APPEND extra_cmake_args "-DCMAKE_TOOLCHAIN_FILE=${CMAKE_TOOLCHAIN_FILE}")
     if(ANDROID)
@@ -68,6 +74,7 @@ macro(build_console_bridge)
   externalproject_add(console_bridge-1.0.1
     URL https://github.com/ros/console_bridge/archive/1.0.1.tar.gz
     URL_MD5 9f5351994c7690b37f88e9f70e478061
+    PATCH_COMMAND patch -p1 < ${CMAKE_CURRENT_SOURCE_DIR}/0001-add-if-BUILD-TESTING.patch
     TIMEOUT 600
     CMAKE_ARGS
       -DCMAKE_INSTALL_PREFIX=${CMAKE_CURRENT_BINARY_DIR}/${PROJECT_NAME}_install
-- 
2.25.1

